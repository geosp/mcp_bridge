{
  "mcpServers": {
    "weather-service": {
      "command": "mcp-bridge",
      "args": ["--config", "weather.json"]
    },
    "database-service": {
      "command": "mcp-bridge",
      "args": ["--config", "database.json"]
    }
  }
}
